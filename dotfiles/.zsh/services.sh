alias start-all-daemons="brew services start postgresql && brew services start memcached && brew services start redis && brew services start rabbitmq"
alias stop-all-daemons="brew services stop postgresql && brew services stop memcached && brew services stop redis && brew services stop rabbitmq"
